<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.whc.mapper.CategoryMapper">
    <select id="findCategoryWithProduct" parameterType="Integer"
            resultMap="CategoryWithProductResult">
        select * from category c,product p
        where c.id = p.category_id
        and c.id=#{id}
    </select>
    <resultMap id="CategoryWithProductResult" type="org.whc.pojo.Category">
        <id property="id" column="id"/>
        <result property="typename" column="typename"/>
        <collection property="productList" ofType="org.whc.pojo.Product">
            <id property="id" column="catefory_id"/>
            <result property="goodsname" column="goodsname"/>
            <result property="price" column="price"/>
        </collection>
    </resultMap>
</mapper>